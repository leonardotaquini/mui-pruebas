import { Typography } from "@mui/material"
import { DashboardLayout } from "../layout/DashboardLayout"

export const MetricsPage = () => {
  return (
    <DashboardLayout>
        <Typography variant="h4" align="center" mt={2}> Metrics</Typography>
    </DashboardLayout>
  )
}
